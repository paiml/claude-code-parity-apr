# Pin lock — claude-code-parity-apr ↔ aprender contract sync
#
# Records the upstream commit + content hash of the authoritative contract
# this repo gates against. Refreshed by CI step `scripts/pin-check.sh`,
# which fails the build if the recorded sha256 no longer matches the local
# contract bytes.
#
# Lifecycle:
#   - On every commit:   pre-commit hook + CI re-run pin-check.
#   - On contract bump:  bump aprender_commit + contract_sha256 atomically.
#   - On aprender PR merge: refresh aprender_commit to the squash-merge hash.
#
# See:
#   - contracts/claude-code-parity-apr-v1.yaml § companion_repo.contract_pin
#   - docs/specifications/claude-code-parity-apr-poc.md § Companion-repo invariants
#   - FALSIFY-CCPA-012 (pv_contract_gate_on_commit)

aprender_repo:        paiml/aprender
aprender_commit:      f5cfbb370
aprender_branch:      feat/claude-code-parity-apr-poc-spec
aprender_pr:          1078
aprender_pr_state:    OPEN
aprender_path:        contracts/claude-code-parity-apr-v1.yaml
contract_path:        contracts/claude-code-parity-apr-v1.yaml
contract_sha256:      d37f41f9f102398df57a4bad94ec89b99ef2a556831cc243dd488d288bd931f2
last_synced_utc:      2026-04-27T08:30:00Z
note: |
  M16 — contract bumps v1.3.0 → v1.4.0 (minor). FALSIFY-CCPA-007
  flips from informational-until-17/17 to HARD-BLOCKING on 15/15
  reachable rows. The 2 OOS rows (keyboard-shortcuts, status-line)
  are now formally declared in the contract's FALSIFY-CCPA-007
  `oos_rows:` block (with reasoning) and the ccpa-coverage CLI
  exposes `--oos-rows` to consume that list at gate time.

  CI workflow adds a sparse-checkout of paiml/aprender@main to
  pull the canonical apr-code-parity-v1.yaml and runs
    ccpa coverage --apr-code-parity-yaml ... --oos-rows ...
  as a hard-blocker.

  contract_sha256 above is the v1.4.0 hash. Refresh `aprender_commit`
  to the squash-merge hash on aprender@main when the matching
  aprender-side mirror PR merges; contract_sha256 should be
  byte-identical across that merge.
