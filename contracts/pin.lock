# Pin lock — claude-code-parity-apr ↔ aprender contract sync
#
# Records the upstream commit + content hash of the authoritative contract
# this repo gates against. Refreshed by CI step `scripts/pin-check.sh`,
# which fails the build if the recorded sha256 no longer matches the local
# contract bytes.
#
# Lifecycle:
#   - On every commit:   pre-commit hook + CI re-run pin-check.
#   - On contract bump:  bump aprender_commit + contract_sha256 atomically.
#   - On aprender PR merge: refresh aprender_commit to the squash-merge hash.
#
# See:
#   - contracts/claude-code-parity-apr-v1.yaml § companion_repo.contract_pin
#   - docs/specifications/claude-code-parity-apr-poc.md § Companion-repo invariants
#   - FALSIFY-CCPA-012 (pv_contract_gate_on_commit)

aprender_repo:        paiml/aprender
aprender_commit:      f5cfbb370
aprender_branch:      feat/claude-code-parity-apr-poc-spec
aprender_pr:          1078
aprender_pr_state:    OPEN
aprender_path:        contracts/claude-code-parity-apr-v1.yaml
contract_path:        contracts/claude-code-parity-apr-v1.yaml
contract_sha256:      2038755e79f1f30bd1d0977aed1cc35d29362a0d392608eae51c8f85d7ce1ade
last_synced_utc:      2026-04-27T07:30:00Z
note: |
  M14 corpus expansion. Contract version held at v1.2.0 (informational
  measured_parity append, no semver bump). New status_history entry
  records 17-fixture corpus, aggregate=1.0000, parity-matrix coverage
  10/17 → 13/17 (newly covered: worktree-isolation, configuration-ladder,
  managed-org-policy). Four uncovered rows (hooks, skills,
  keyboard-shortcuts, status-line) tracked under
  remaining_uncovered_classification with explicit schema-extension
  / out-of-scope reasoning.

  contract_sha256 above is the v1.2.0+M14-append hash. Refresh
  `aprender_commit` to the squash-merge hash on aprender@main when
  the matching aprender-side mirror PR merges; `contract_sha256`
  should be byte-identical across that merge (we keep the two
  contract files in lock-step).
